import { FilterbylocalityPipe } from './filterbylocality.pipe';

describe('FilterbylocalityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbylocalityPipe();
    expect(pipe).toBeTruthy();
  });
});
