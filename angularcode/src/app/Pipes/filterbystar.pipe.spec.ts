import { FilterbystarPipe } from './filterbystar.pipe';

describe('FilterbystarPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbystarPipe();
    expect(pipe).toBeTruthy();
  });
});
