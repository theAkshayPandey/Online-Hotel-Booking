import { EmployeeDataResult } from './employee-data-result';

describe('EmployeeDataResult', () => {
  it('should create an instance', () => {
    expect(new EmployeeDataResult()).toBeTruthy();
  });
});
